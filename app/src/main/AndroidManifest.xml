<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="tyler.HealthAppV3">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:background="@color/colorPrimary"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_circle_plus"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".SundayActivity"></activity>

        <activity android:name=".SelectDayActivity" />
        <activity android:name=".BMIActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".RoutinesActivity" />
        <activity android:name=".UpdateRoutineActivity"/>
        <activity android:name=".NutritionCalculatorActivity" />
        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>